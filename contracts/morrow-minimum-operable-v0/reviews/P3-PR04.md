# Revisão adversarial local — P3-PR04

- estado: `GREEN_CANDIDATE`
- base integrada: `5624e2affd521368bdea921dc19e549b5c728776`
- kickoff contratual: `325b08ca28456d565a8a42095d2ddc9a2638aa35`
- candidate de código/teste: `ff744d2ad8bc41435d9b956a9518b4486f695501`
- branch: `mvo/p3-pr04-conpty-multiplexing`
- PR remota: pendente
- head remoto revisado: pendente

## Objetivo verificado

Provar duas ou mais sessões Windows ConPTY simultâneas em workspaces distintos, com identidade e input preservados, colisões recusadas, timeout/cancel/falha distinguíveis e cleanup completo da raiz e de seus descendentes. Nenhum processo, workspace ou terminal do operador pode ser adotado.

## Entregáveis revisados

- `src/terminal-session.ts`: reserva pós-`await`, capacidade configurável e teto fail-closed da capacidade ConPTY medida;
- `src/probes/conpty-multiplex.ts`: soak real concorrente, identidades, input, timeout/cancel, colisões, PIDs, órfãos, cleanup e deadline;
- `test/terminal-session.test.ts`: corrida concorrente de terminal/agente/workspace/capacidade e preservação da concorrência process-pipes;
- `test/windows-conpty-backend.test.ts`: teto não provado recusado e probe real incorporado ao gate Windows;
- `runtime/CONPTY_MULTIPLEXING.md`: arquitetura, evidência e limites.

## Passe de arquitetura e segurança

| pergunta/ataque | resultado |
|---|---|
| Duas sessões usam ConPTY real ao mesmo tempo? | `GREEN` — cada par ativo apresenta `windows-conpty/conpty-vt`, terminal completo e PIDs próprios |
| Uma corrida assíncrona pode duplicar terminal ou agente? | `GREEN` — as identidades são rechecadas depois da resolução canônica do workspace e antes da reserva síncrona |
| Dois agentes podem compartilhar o mesmo workspace? | `GREEN` — a raiz canônica ativa é exclusiva e a corrida possui contraprova |
| Input de uma sessão cruza para outra? | `GREEN` — tokens exclusivos são enviados simultaneamente e procurados também nas saídas das demais sessões |
| Timeout e cancel viram o mesmo estado? | `GREEN` — cada rodada exige um `timed_out` e um `stopped`, com flags correspondentes; exit não zero já permanece provado como `failed` no gate ConPTY |
| Stop/timeout deixam descendente vivo? | `GREEN` — cada uma das 12 sessões cria descendente e todos os 24 PIDs ficam inexistentes antes da remoção da fixture |
| Uma faixa nativa instável pode ser habilitada silenciosamente? | `GREEN` — Windows ConPTY recusa configuração acima de duas sessões, capacidade efetivamente repetida nesta unidade |
| O limite ConPTY reduz o backend processual? | `GREEN` após correção — process-pipes conserva concorrência sem teto implícito e prova três sessões simultâneas |
| O probe pode aguardar para sempre? | `GREEN` após hardening — deadline de 32 s tenta parar somente sessões próprias e força resultado vermelho |
| O probe toca diretório externo? | `GREEN` — raiz autocontida em `.morrow-test-tmp`, escopo validado antes de qualquer remoção |

## Achados e resolução

1. A primeira contraprova reproduziu um TOCTOU: duas chamadas concorrentes atravessavam as verificações anteriores ao `await` e ambas podiam iniciar. A segunda barreira pós-resolução agora recusa terminal, agente e workspace duplicados.
2. O primeiro soak com quatro sessões ConPTY simultâneas passou uma vez, depois a suíte terminou com `3221225477` (`0xC0000005`) e repetições ficaram penduradas. A prova foi reduzida ao mínimo contratual de duas simultâneas e essa capacidade passou a ser o teto explícito do backend Windows medido.
3. A primeira aplicação do teto também limitava `process-pipes` sem evidência. A revisão removeu o efeito lateral: o backend processual preserva a concorrência anterior e três sessões simultâneas têm contraprova.
4. O probe dedicado dependia de timeout externo para uma regressão de handles. Foi adicionado deadline próprio, com stop endereçado somente às sessões da fixture e falha explícita por estágio.
5. O probe usava inicialmente o temp do sistema. A raiz passou a ser `.morrow-test-tmp` dentro do repositório, e a remoção valida mecanicamente que a fixture continua abaixo dela.

## Provas locais no candidate

- repetição direta do soak após limitar a concorrência: três execuções consecutivas verdes;
- `npm run probe:conpty-soak` final → 3 rodadas, 12 sessões, 6 completas, 3 timeout, 3 canceladas, 12 colisões recusadas, 24 PIDs distintos inexistentes, input isolado e fixture removida;
- `npm run probe:conpty` → `7/7`;
- testes focados de sessão → `15/15`;
- `npm test` → `159/159`;
- `npm run probe:codex-conpty` → verde, quota-session/ConPTY real, cwd/identidades/output ao vivo e nenhuma mutação;
- `npm run probe:codex-quota` → verde, quota-session sem mutação;
- `git diff --check` → verde.

## Limites e gate remoto

- aumento da capacidade ConPTY exige nova medição; esta PR não presume que versões futuras da dependência tenham o mesmo teto;
- persistência/redaction/replay e UI pertencem a P4/P5;
- `GREEN_CANDIDATE` não equivale a `PROVEN`: ainda faltam publicação, conferência mecânica do head/base, revisão adversarial do diff remoto exato e integração seguida de regressão.

## Parecer local

`P3-PR04: GREEN_CANDIDATE`. O candidate `ff744d2` cumpre o mínimo de concorrência real com contenção fail-closed e corrige todos os achados locais. Publicação e revisão remota do SHA exato estão autorizadas; `PROVEN` permanece proibido até esses gates e o pós-merge.
