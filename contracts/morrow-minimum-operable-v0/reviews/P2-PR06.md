# Revisão de arquitetura e segurança — P2-PR06

- estado: `GREEN_CANDIDATE`
- base integrada: `9b96a8b`
- candidate de código/teste: `d2c54de4f02e8612b137e858e19f370c9ad1e142`
- branch: `mvo/p2-pr06-recovery`
- PR remota: pendente
- execução/revisão: `GPT-5.6 Sol / xhigh`, acesso da sessão do operador, sem API

## Objetivo verificado

Implementar reconnect, retry idempotente, fila, checkpoint e estado online/offline do Local Worker. A prova obrigatória precisa matar/reiniciar um processo sem duplicar efeito e fazer toda pendência retomar com segurança ou bloquear com causa explícita.

## Entregáveis revisados

- `src/worker-recovery.ts`: coordenador, fila/checkpoint atômicos, liveness, retry e restart fail-closed;
- `src/worker-protocol.ts`: reutilização pública do decoder estrito de `ControlDispatchBody` para validar snapshot;
- `test/worker-recovery.test.ts`: onze cenários funcionais e adversariais;
- `test/fixtures/worker-recovery-child.ts`: prova real entre processos, incluindo kill depois do efeito;
- `runtime/WORKER_RECOVERY.md`: protocolo operacional, estados, confiança e limites;
- documentação de runtime/Worker/dispatch reconciliada.

## Passe de arquitetura

| pergunta | resultado |
|---|---|
| PC/Worker offline pode executar? | `GREEN` — aceite durável permanece `queued`; tentativa exige hello seguido de heartbeat autenticado e lease futuro |
| reconnect depende da conversa? | `GREEN` — snapshot versionado reidrata fila, fingerprints, tentativas e causas sem prompt/chat |
| retry pode trocar o efeito? | `GREEN` — idempotency key + fingerprint + dispatch ID recusam rebinding; a chave é preservada em toda tentativa |
| restart pode repetir resultado incerto? | `GREEN` — qualquer `running` reidratado vira `blocked:execution_outcome_unknown_after_restart` antes de reconectar |
| conclusão confirmada pode repetir? | `GREEN` — `completed` permanece terminal; repetição retorna o mesmo estado e não chama o executor |
| a solução antecipou transporte, terminal ou observabilidade? | `GREEN` — tentativa é callback de composição; não há rede, ConPTY, transcript, redaction ou UI |
| área do operador continua separada? | `GREEN` — estado exige raiz absoluta com segmento `.morrow`; fixtures usam somente tmp e nenhum workspace externo |

## Passe de Security Reviewer

| ataque/erro | defesa comprovada |
|---|---|
| dispatch não autenticado ou para outro Worker | protocolo P2-PR01 valida identidade, direção, credencial, scopes, TTL e recipient antes de persistir |
| mesma chave reaponta para outro corpo | `IDEMPOTENCY_CONFLICT`; fingerprint canônico e dispatch ID são vinculados |
| fila cresce sem limite | capacidade fixa recusa item novo e não remove histórico idempotente |
| snapshot é editado/corrompido | schema estrito, hash do body e checksum do estado falham fechados |
| arquivo/ancestral simbólico redireciona escrita | raiz e snapshot simbólicos são recusados antes da operação |
| proof/token/authorization ou output vazam em disco | somente `ControlDispatchBody` e resumo terminal são armazenados; contraprova busca marcadores sensíveis no JSON |
| heartbeat expirado ou de sessão desconhecida libera fila | lease é validado pelo relógio confiável e sessão precisa ter sido anunciada por hello |
| executor lança após possível efeito | item vira `blocked`, Worker vira `offline` e não existe retry automático |
| processo morre após efeito e antes do resultado | checkpoint já está `running`; restart converte em bloqueio explícito sem chamar o executor |
| recusa anterior ao efeito precisa retomar | somente quatro códigos declarados retornam a `queued`; demais recusas são terminais/bloqueantes |

## Achados internos e resolução

O primeiro carregamento encontrou duas falhas mecânicas antes dos testes: parameter property não suportada pelo modo TypeScript nativo do Node e import ausente de `resolve`. Ambas foram corrigidas antes do candidate. A revisão também ampliou as contraprovas para capacity sem evict, corrupção, vazamento de material sensível, lease vencido, sessão não anunciada, retry permitido e resultado de tentativa desconhecido.

Nenhum achado bloqueante conhecido permanece no candidate local. O parecer continua `GREEN_CANDIDATE`, não `PROVEN`, até publicação, revisão do head remoto e merge controlado.

## Provas executadas

- testes focados de recovery: `11/11`;
- suíte completa: `npm test` → `120/120`;
- processo filho real: fila criada em um processo e drenada uma única vez por outro;
- processo filho morto com exit `91` depois de criar o efeito e antes do settlement; novo processo reidratou `blocked` e realizou zero replay;
- corrupção de body/checksum recusada;
- `git diff --check`: verde antes do commit de código;
- fixtures somente sob diretórios temporários com segmento `.morrow`;
- nenhuma rede, credencial, cobrança ou target externo utilizado;
- Enova não foi acessada nem alterada.

## Limites explícitos

- transporte remoto concreto não é implementado; o MVO local-first compõe a fronteira localmente e qualquer topologia remota futura permanece outbound sob `D-004/D-009`;
- eventos canônicos, transcript, redaction e replay de UI permanecem P4;
- ConPTY, input, resize, sinais e multiplexação permanecem P3;
- notificação fora do PC e decisão externa permanecem P6;
- cancel/pause completos e contenção de árvore de processos permanecem nas PRs contratuais próprias;
- o estado durável é uma cerca de runtime, não sandbox do sistema operacional.

## Parecer local

`P2-PR06: GREEN_CANDIDATE`. O objetivo e sua prova de kill/restart estão satisfeitos localmente. Próxima ação permitida: publicar somente esta branch, abrir a PR contra `phase-2/runtime-v0`, revisar o head remoto e corrigir qualquer achado antes de autorizar merge. P3 permanece proibida.
