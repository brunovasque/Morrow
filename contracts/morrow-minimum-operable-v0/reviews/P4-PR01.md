# Revisão adversarial local — P4-PR01

- estado: `GREEN_CANDIDATE`
- base integrada: `461bb4043da86e27e1b075c2d2aef9e296ff1495`
- kickoff contratual: `aafb4af96e3a02fd2087547c6d5d0faf443c8d5d`
- candidate de código/teste: `6bbe7cd653feae30c0a06e427b729714189bec54`
- hardening após revisão remota: `375da191a92157dae94abce7df3f1b7d7b01db2c`
- branch: `mvo/p4-pr01-live-activity-events`

## Objetivo verificado

Definir schema versionado e projector puro do Live Activity Feed capaz de reconstruir estados mecânicos sem inferir atividade a partir de texto ou ausência de evento. A unidade precisa distinguir dispatch, gate, tool, process, waiting-lock, waiting-quota, waiting-owner, blocked, failed e done.

## Entregáveis revisados

- `schema/live-activity.v1.schema.json`: envelope JSON fechado `morrow.live-activity/1.0`;
- `src/live-activity.ts`: tipos, decoder estrito e projector determinístico por contrato;
- `test/live-activity.test.ts`: schema drift, estados, identidade, ordem, causalidade e input hostil;
- `runtime/LIVE_ACTIVITY_FEED.md`: semântica, reconstrução e fronteiras das próximas PRs;
- `runtime/README.md`: entrada para a documentação canônica.

## Passe adversarial

| pergunta/ataque | resultado |
|---|---|
| O feed pode mostrar atividade sem evento? | `GREEN` — input vazio produz entries/activities vazios e sequence 0 |
| Todos os estados mínimos AC-21 são reconstruídos? | `GREEN` — os dez estados são projetados e três finais distintos ficam materializados |
| Schema/versão/campos podem derivar silenciosamente? | `GREEN` — versão exata, campos exatos e JSON schema alinhado a constantes executáveis |
| Evento pode trocar contrato/step/agente/terminal/workspace durante a atividade? | `GREEN` — identidade completa e correlation id ficam presos ao primeiro dispatch |
| Array reordenado, gap, duplicata ou relógio regressivo passa? | `GREEN` — todos falham com código e índice do evento |
| Writer pode declarar `from` stale ou continuar após terminal? | `GREEN` — state mismatch e transição após blocked/failed/done são recusados |
| Causalidade pode saltar para outro head? | `GREEN` — primeiro dispatch exige causation null; eventos seguintes apontam ao último event id da atividade |
| Fonte `process` pode fabricar `waiting-quota`? | `GREEN` após correção — sourceKind é vinculado mecanicamente ao estado visível |
| Accessor/prototype/Proxy hostil executa ou vaza erro? | `GREEN` após correção — objetos plain/data-only e erro sanitizado sem invocar getter |
| Objeto aceito pode ser alterado depois? | `GREEN` — evento/projeção são cópias destacadas e profundamente congeladas |
| Esta PR antecipou storage/replay/API/UI? | `GREEN` — limites P4-PR02/03/04 e P5 permanecem explícitos |

## Achados e resolução

1. O primeiro schema validava `sourceKind` e estado separadamente; um produtor poderia rotular fonte processual como espera de quota. A validação passou a exigir a matriz fonte→estado.
2. A inspeção de objetos plain recusava accessors sem executá-los, mas um Proxy podia lançar em `getPrototypeOf`/`ownKeys`. As APIs públicas agora convertem qualquer exceção de inspeção em erro estável e sanitizado.
3. Sequência global e `from` preservavam ordem, mas não ligavam explicitamente cada transição ao head da atividade. `correlationId`/`causationId` tornaram a cadeia causal obrigatória e possuem contraprova de salto.
4. Texto livre foi excluído do schema v1 para não criar um caminho de persistência/exibição antes do Stream Redactor P4-PR02. `reasonCode` e ids estruturados são limitados e não substituem transcript.
5. A revisão do head publicado encontrou que o alinhamento executável não conferia todas as listas `required` e que um elemento accessor do array podia ser lido antes do decoder. `375da19` valida os campos requeridos aninhados e lê cada elemento somente por own data descriptor; getter de array tem contraprova com zero chamadas.

## Provas locais

- `node --experimental-strip-types --test test/live-activity.test.ts` → `8/8`;
- `npm test` → `172/172`;
- `git diff --check` → verde;
- nenhum target externo, terminal/projeto do operador, segredo, rede ou processo adicional foi usado.

## Limites

- persistência, redaction e transcript: P4-PR02;
- cursores/replay/reidratação/liveness: P4-PR03;
- produtores/API e duas sessões reais: P4-PR04;
- dashboard e visualização: P5.

## Parecer local

`P4-PR01: GREEN_CANDIDATE` com candidate `6bbe7cd653feae30c0a06e427b729714189bec54` e hardening `375da191a92157dae94abce7df3f1b7d7b01db2c`. Publicar o novo head da PR #16, revisar o diff remoto exato e revalidar antes de `GREEN/PROVEN`.
