# Revisão adversarial local e remota — P4-PR02

- estado: `GREEN_CANDIDATE`
- base integrada: `3657a070e5dc6b1e7b78fa1804761440c55efffc`
- kickoff contratual: `45f32a86fc8061a9f6c5c7a02c5aa6278ea6a497`
- candidate inicial invalidado: `5f9e523b12bb946ebb07ec2fa288bf341dc24cd8`
- hardening de trust boundaries: `69fce7413f78de222be67f7e93d49b29584e945f`
- contraprova final de load: `4773a5e64433dbf504031a06b2ac5ee2c5ffdbe1`
- head remoto anterior invalidado: `e525eb85589a3ae74ded371c039c6a61418184a7`
- correções da revisão remota: `2c781b65520442bd455db7a33eca50c3f4adbbd8`
- hardening adversarial de line boundary: `3824d1a26d8731e5e658c466fdadf3c864c9b23e`
- scanner de assignment com escape: `004b0d9bd767f5cfe2bb4efb7bf4f62e5a5725e6`
- prefixo de assignment incompleto: `778f6a5258d53327f138818d7ab5fb334de9d40d`
- segundo passe remoto revisado: `dbcd79052ace98720e753516498f417c105c8f29`
- correções do segundo passe remoto: `07e1c2585cf56b0b5241361be6f197ef2723bca0`
- terceiro passe remoto revisado: `80bd3948c2cfe415d30a19cd302d3c09eeb2dd49`
- correções do terceiro passe remoto: `20044db20e97bd3135fcf5304526e2bfa0da3462`
- quarto passe remoto revisado: `8d5f49f37d1a572ca4e385b526bfb91aefddc8de`
- correções do quarto passe remoto: `35079abf0bdc7a4e0793ffba3da190a3ba7485c5`
- head da revalidação final com P1: `7cde49eaad60992906200f66de7dc863f2fd0f8e`
- correção categórica camelCase: `1b7a223077de98ba2b198d80e7dcacaafd7d3a85`
- head remoto com review de backspace/dotted key: `0197429dc8fc77ee12d002752b23ab67aaff75c5`
- correções de terminal rewrite/dotted key: `8819be728400d3fc35a3f354d0ab421635db9989`
- head remoto com review de multiline assignment: `65786cbd1f26e176a88d49874b9604c4a609c9e6`
- correção de whitespace estrutural: `c8465c282fb07896f40855ab71f612600bca2c7c`
- head remoto com review de spaced key/YAML block: `2934ed334c04f3143692ab0d75ab0d8e431f9536`
- correções estruturais spaced key/YAML block: `024c5a94036c228de607832704ee9c979cf0c2e9`
- head bloqueado pela revisão independente de escaping e pelos P1 remotos posteriores: `1d9316165ec9952c25d1e3551de146ea3382fa66`
- correções estruturais de escaping/terminal/YAML e candidate de código: `79382d421a9a6e9df2956007fb701d32d00c5952`
- GitHub PR: `#18`
- branch: `mvo/p4-pr02-stream-redaction-transcript`

## Objetivo verificado

Implementar Stream Redactor, política explícita de acesso/retenção e transcript persistente de forma que texto não confiável nunca alcance storage ou futura UI antes da redaction. Input de terminal é sensível por padrão e a prova usa somente canários sintéticos.

## Passe adversarial

| pergunta/ataque | resultado |
|---|---|
| segredo literal dividido entre chunks aparece em fragmento ao vivo ou snapshot? | `GREEN` — lookbehind segura o intervalo; nenhum fragmento, view ou arquivo contém o canário |
| ANSI/OSC/backspace/zero-width/bidi pode separar caracteres e reconstruir o segredo no renderer? | `GREEN` após correção — visão textual mapeada redige o intervalo bruto inteiro e remove controles antes do retorno |
| stdin pode ser liberado em claro por flag ou commit direto? | `GREEN` — stream `input` sempre vira marcador; commit exige capability interna e revalida a invariável |
| `private` apagado pelo strip-only expõe segredo pendente, estado ou bypass de acesso? | `GREEN` após correção — valores, buffers e estado usam campos privados `#`; writer/commit exigem capabilities não exportadas |
| dois processos, close concorrente, crash ou recuperação simultânea de lease stale corrompem/liberam cedo a raiz? | `GREEN` após correção — lease usa hard link; recovery usa named-pipe mutex liberado pelo Windows em crash, relê ownership e somente uma abertura vence |
| retenção pode ser contornada por count, bytes, idade ou temporários de crash? | `GREEN` — as três cercas removem oldest deterministicamente e reopen limpa apenas temporários Morrow exatos |
| assignment com aspas, espaço e mais de 4.096 caracteres pode vazar antes da aspa final ou ao receber newline? | `GREEN` após correção — o intervalo incompleto cruza o cut; fechamento ou line boundary redige a faixa inteira antes de liberar o restante |
| aspa escapada pode ser confundida com fechamento e liberar o restante? | `GREEN` após correção — scanner backslash-aware consome o par escapado e mantém o assignment até quote real ou line boundary |
| espaços acima do holdback antes do separador/valor podem expulsar a chave sensível? | `GREEN` após correção — o scanner começa na chave e mantém prefixos incompletos até `:`/`=`, valor ou line boundary |
| identificadores como `DB_PASSWORD` e `AWS_SECRET_ACCESS_KEY` escapam do boundary `\b`? | `GREEN` após correção remota — o scanner reconhece componentes sensíveis delimitados por underscore e redige o assignment inteiro |
| propriedades quoted como `{"password":"..."}` escapam do scanner? | `GREEN` após correção remota — quote simétrica em volta da chave é consumida antes de exigir `:`/`=` e toda a propriedade sensível é redigida |
| `Authorization: Basic/Digest credential` libera o credential após redigir apenas o scheme? | `GREEN` após correção remota — authorization não quoted é retida/redigida até CR/LF, inclusive em chunks longos |
| chaves camelCase sensíveis como `clientSecret`, `accessToken`, `refreshToken`, `apiKey` ou `serviceCredential` escapam, ou substrings não sensíveis sofrem redação indevida? | `GREEN` após correção — segmentação lexical cobre snake/kebab/camel/PascalCase; canários divididos em chunks não chegam ao retorno, view ou disco, enquanto `clientSecretary`, `accessTokenizer`, `passwordlessMode`, `credentialedUser` e `apiKeyboardLayout` permanecem visíveis |
| backspace pode construir visualmente uma chave sensível depois que o normalizador apenas remove o controle? | `GREEN` após correção — buffer de células aplica overwrite antes do matcher e mapeia toda a sequência bruta; backspace em chunks não libera canário e `versX\bion=1` permanece `version=1`; movimentos amplos de cursor ficam fail-closed até CR/LF |
| chave quoted composta como `request.headers.authorization` escapa porque o matcher começa apenas no componente após o ponto? | `GREEN` após correção — o scanner consome o identificador dotted completo e segmenta cada componente; authorization/clientSecret são redigidos no retorno/view/disco e `contentType`/`clientSecretary`/`accessTokenizer` permanecem visíveis |
| pretty JSON/YAML com valor sensível após CR/LF libera o valor porque a chave é redigida e esquecida na linha anterior? | `GREEN` após correção — assignment sem valor permanece pendente através de whitespace estrutural/chunks até o valor quoted/plain; três canários multiline ficam ausentes de fragmentos/view/disco e `safeField` multiline permanece visível |
| chaves quoted com espaços como `API Key`/`Client Secret` escapam, ou frases semelhantes sofrem falso positivo? | `GREEN` após correção — scanner quoted consome a chave estrutural inteira e segmenta whitespace; canários em chunks ficam ausentes de live/view/disco, enquanto `API Keyboard`, `Client Secretariat` e `Access Tokenizer` permanecem intactos |
| YAML block scalar `|`/`>` encerra a faixa no indicador e libera linhas indentadas? | `GREEN` após correção — parser limitado inclui header, comentários/modificadores e linhas blank/indentadas até a primeira dedentada; scalars literal/folded em chunks são redigidos e campos seguintes permanecem visíveis |
| assignment PowerShell válido com backtick ou quote duplicada pode encerrar o valor na quote escapada? | `GREEN` após vermelho reproduzido — parser PowerShell confirmou 0 erros; scanner contextual consome backslash, backtick e quote duplicada sem liberar canário entre chunks; live, inspect e disco ficam limpos e chaves semelhantes permanecem visíveis |
| APC/DCS/PM/SOS pode inserir payload invisível no meio da chave, especialmente além do holdback? | `GREEN` após vermelho reproduzido — string controls de 7-bit/C1 são consumidos até ST; sequência incompleta mantém a linha pendente/fail-closed e o matcher recebe a visão visível completa |
| consumer JavaScript pode sobrescrever `ranges`/`redact` no prototype ou na instância e desligar as duas cercas? | `GREEN` após vermelho reproduzido — matcher usa método `#` real; prototype e instância do redactor são congelados e a tentativa de shadow/override falha mecanicamente |
| CSI/CR que reescreve chave já emitida pode deixar o valor anterior fora do range? | `GREEN` após vermelho reproduzido — comando completo ou incompleto fecha desde o início da linha afetada; CRLF continua sendo quebra estrutural e linhas seguras separadas permanecem visíveis |
| YAML quoted multiline, plain scalar com espaços ou block em sequence pode liberar segredo ou consumir sibling seguro? | `GREEN` após vermelho reproduzido — quoted respeita continuação/escaping, plain usa boundary de scalar e block calcula indent também após `-`; chunks não contêm canários e comentários/dedent/siblings seguros permanecem |
| policy drift ou snapshot adulterado com checksum recalculado é aceito? | `GREEN` — acesso/retenção/policy id divergentes falham; cada conteúdo é redigido novamente no load e respeita `maxRecordBytes` individual |
| timestamps futuros ou fora de ordem podem impedir retenção por idade? | `GREEN` após correção remota — records precisam ser não decrescentes e nunca posteriores a `updatedAt` |
| snapshot checksummed pode atribuir record a writer fora da allowlist? | `GREEN` após correção remota — todo `writerId` reidratado precisa pertencer a `access.writerIds` |
| literal configurado pode redigir os próprios marcadores gerados? | `GREEN` após correção remota — qualquer literal que contenha ou seja substring dos dois marcadores é recusado na política |
| symlink de snapshot, junction ancestral ou diretório alheio é seguido/adotado ou sofre escrita antes da recusa? | `GREEN` após correção — cada ancestral existente é validado antes de `mkdir`; criação é componente a componente e casing só é dobrado no Windows |
| objetos hostis executam getters ou propagam seus detalhes? | `GREEN` — dados próprios exatos são lidos por descriptors e falhas públicas retornam códigos sanitizados |
| replay/cursores/API/UI foram antecipados? | `GREEN` — permanecem explicitamente P4-PR03/P4-PR04/P5 |

## Achados e resolução

1. O candidate `5f9e523` comparava texto bruto; escapes e caracteres invisíveis podiam ocultar um canário do matcher e fazê-lo reaparecer visualmente. `69fce74` passou a normalizar C0/C1, VT/ANSI/OSC e format controls com mapa para o intervalo bruto.
2. A lease criada diretamente com `wx` tinha janela de crash para lock vazio. A publicação agora grava temp completo e cria hard link atômico; restos de crash com nome exato são removidos somente após ownership.
3. Campos `private` TypeScript não eram cerca runtime no Node strip-only, e o método interno de commit era alcançável. Estado, literais e buffers migraram para `#`; writer e commit exigem capabilities fechadas no módulo.
4. O snapshot usava `stat`, que seguia symlink. O load agora usa `lstat`, e a raiz só nasce vazia, recebe marcador Morrow e recusa qualquer entrada alheia ou junction/canonicalização divergente.
5. Chamadas simultâneas de `close()` podiam retornar antes de a primeira liberar a lease. Uma única `closeOperation` compartilhada espera toda mutação já admitida.
6. A contraprova final recalcula um checksum válido sobre snapshot contendo o canário; o load ainda recusa porque reaplica o redactor ao conteúdo.
7. A revisão automatizada remota encontrou vazamento de assignment quoted incompleto além do holdback. `2c781b6` separa reconhecedores quoted/unquoted, mantém a faixa sem aspa até o fim e contrapõe mais de 5.000 caracteres no espelho, view e disco.
8. A revisão remota encontrou bypass de `maxRecordBytes` por snapshot com checksum recalculado. O load agora mede cada `content` em UTF-8 e a contraprova recusa 33 bytes sob política de 32.
9. A revisão remota encontrou colisão idempotente com literais como `REDACT`. A normalização de política agora recusa sobreposição em ambas as direções com `[REDACTED]` e `[SENSITIVE_INPUT_REDACTED]`.
10. O passe adversarial sobre `2c781b6` reproduziu um vazamento quando a faixa quoted longa recebia newline sem aspa final. `3824d1a` trata CR/LF como terminador seguro da faixa e a mesma contraprova cobre quote fechado e line boundary em fragmento, view e disco.
11. O passe seguinte reproduziu vazamento após `\"`: o regex confundia aspa escapada com fechamento. `004b0d9` substitui as alternativas por scanner backslash-aware e a contraprova mantém mais de 5.000 caracteres protegidos após a aspa escapada.
12. O passe de chunking reproduziu perda da chave quando mais de 4.096 espaços precediam o separador/valor. `778f6a5` inicia a faixa na chave e conserva prefixos incompletos; a contraprova divide um assignment com 5.000 espaços antes do canário.
13. O segundo review remoto mostrou que `\b` não encontra componentes após `_`. `07e1c25` reconhece nomes compostos e contrapõe `DB_PASSWORD` e `AWS_SECRET_ACCESS_KEY`.
14. Duas recuperações podiam ler a mesma lease stale e uma remover a lease nova da outra. `07e1c25` serializa recovery com guard atômico, relê PID/token e contrapõe duas aberturas concorrentes com somente um vencedor.
15. A raiz inexistente era criada recursivamente antes da verificação canônica, permitindo mutação através de junction. `07e1c25` valida o ancestral existente antes de criar cada componente; a contraprova confirma que o target controlado permanece vazio.
16. O caminho canônico era sempre comparado sem case, comportamento inseguro em host case-sensitive. A comparação agora dobra casing somente em `win32` e é exata nos demais hosts.
17. Snapshot checksummed aceitava `occurredAt` futuro/fora de ordem e quebrava a premissa da retenção head-first. O load agora exige ordem temporal não decrescente e teto em `updatedAt`, com ambas as adulterações contraprovadas.
18. O terceiro review mostrou que um recovery guard em arquivo poderia sobreviver ao crash e bloquear a raiz indefinidamente. `20044db` usa mutex de named pipe derivado do hash da raiz; o Windows libera o endpoint com a morte do processo. A contraprova mantém o mutex em processo filho, confirma bloqueio enquanto vivo, mata/aguarda o filho e prova recovery posterior.
19. O terceiro review mostrou que snapshot checksummed aceitava `writerId` sintaticamente válido, mas não autorizado. `20044db` reaplica `access.writerIds` no load e contrapõe `intruder` com checksum recalculado.
20. O quarto review mostrou que a quote final da chave JSON impedia reconhecer o separador. `35079ab` aceita chave quoted simétrica, inclui a quote inicial na faixa e contrapõe retorno e snapshot sem `json-only-secret`/`json-persist-canary`.
21. O quarto review mostrou que `Authorization: Basic credential` redigia somente o scheme. `35079ab` trata authorization como campo sensível até CR/LF e contrapõe credenciais Basic no retorno ao vivo e no disco.
22. A revalidação final do head `7cde49e` mostrou que a regex orientada a underscores não classificava chaves camelCase; `{"clientSecret":"hunter2secret"}` podia alcançar retorno e disco. `1b7a223` troca a classificação por segmentos lexicais, cobre cinco famílias sensíveis em chunks e contrapõe cinco substrings não sensíveis sem ampliar a fronteira do Stream Redactor.
23. O mesmo passe observou que uma implementação hostil de `clock` ainda pode lançar durante a conversão do valor, fora do `try` que sanitiza a chamada. O achado não demonstrou vazamento para retorno ou disco e foi separado como `D-014`, direcionado ao hardening de reidratação/liveness da P4-PR03.
24. O review do head `0197429` reproduziu `passX\bword=hunter2secret`: remover backspace sem aplicar overwrite produzia `passXword` no matcher e liberava o valor. `8819be7` usa cursor/células para backspace e fail-closed por linha para CR/CSI cursor-changing; canários em chunks ficam ausentes do retorno, view e disco, e o negativo `version=1` é preservado.
25. O mesmo review reproduziu `{"request.headers.authorization":"Basic dXNlcjpwYXNz"}`. `8819be7` inclui ponto no identificador completo e na segmentação lexical; chaves dotted com componentes `authorization` e `clientSecret` são redigidas, mas componentes apenas parecidos continuam visíveis.
26. O comentário TOCTOU sobre `lstat` seguido por `readFile(path)` não apresentou canário de vazamento pelo caminho de retorno/disco e exige hardening do boundary de reidratação. Foi registrado em `D-015` para P4-PR03, sem ampliar esta rodada.
27. O review do head `65786cb` reproduziu `{"password":\n"hunter2"}`: o scanner encerrava a faixa no primeiro CR/LF depois do separador. `c8465c2` atravessa whitespace estrutural até o primeiro valor e contrapõe JSON CRLF, dotted JSON e YAML plain divididos em chunks no retorno, view e disco, preservando um campo multiline não sensível.
28. O mesmo review apontou `redactionCount` reidratado incompatível com o conteúdo. O achado falsifica metadado de auditoria, mas não demonstrou canário de conteúdo no retorno/disco; foi registrado como `D-016` para validação de reidratação na P4-PR03.
29. O review do head `2934ed3` reproduziu `{"API Key":"hunter2"}` e `{"Client Secret":"hunter2"}`. `024c5a9` adiciona scanner da chave quoted inteira e segmentação por whitespace; contraprovas em chunks cobrem retorno/view/disco e três frases similares negativas.
30. O mesmo review reproduziu `password: |\n  hunter2`. `024c5a9` mantém literal/folded block scalars e suas linhas indentadas dentro da faixa até dedent; testes cobrem `|-` com comentário e `>` através de chunks, preservando os campos safe posteriores.
31. O comentário sobre lease stale bloqueada por reutilização do PID descreve risco de disponibilidade/recovery, sem canário no caminho live/disco. Foi registrado como `D-017` para liveness/reidratação da P4-PR03.
32. Uma revisão independente sem alterações no worktree bloqueou `1d93161` com sintaxe PowerShell válida: `$password = "alpha`"..."`. O parser retornou zero erros, mas a quote após backtick era tomada como fechamento e o canário apareceu em retorno live, `inspect()` e `transcript-v1.json`. `79382d4` introduz scanner contextual de escaping (backslash, backtick e quote duplicada), mantém o range entre chunks e contrapõe também variável scoped e chaves semelhantes não sensíveis.
33. Reviews remotos do mesmo head reproduziram APC/DCS/PM, override de prototype e cursor rewrite sobre conteúdo anterior da linha. As contraprovas locais confirmaram os cinco canários antes do write; `79382d4` consome string controls até ST, trata controles incompletos/cursor como fail-closed desde o início da linha e move o matcher para método privado real com prototype/instância congelados.
34. O mesmo head foi contraprovado para YAML quoted multiline e plain scalar com espaços; ambos liberavam continuação depois que a chave já havia sido removida. O scanner agora distingue contexto PowerShell/YAML, atravessa continuação YAML válida, encerra plain scalar por linha/flow/comment e preserva campos seguros. O ajuste de block scalar inclui a indentação introduzida por sequence item e impede consumir o sibling no dedent.

## Provas locais

- `node --experimental-strip-types --test test/stream-transcript.test.ts` → `21/21`;
- `npm test` → primeira execução `192/193`, somente `conpty_soak_native_host_not_isolated` já coberto por `D-013`; controle `npm run probe:conpty-soak` → 3 rodadas/12 sessões/12 PIDs root/descendant/native-host/50 eventos/`noOrphans=true`/fixture removida; repetição integral → `193/193`;
- contraprova exata pós-fix → PowerShell parser 0 erros; canário ausente de cada fragmento live, `inspect()` e disco; negativo seguro preservado; string control/cursor/YAML/prototype também verdes;
- `git diff --check` → verde;
- `npm run contract:reconcile` → `READY_FOR_EXECUTION`, `P4-PR02`, `START_P4_PR02`;
- zero snapshots `transcript-v1.json` residuais sob `.morrow-test-tmp` após a suíte;
- nenhuma credencial real, rede, target externo, terminal/projeto do operador ou diretório fora de `D:\Morrow` foi usado.

Execuções anteriores tiveram colisões de PID/native host depois do encerramento. Nesta rodada, a primeira suíte ficou `192/193` por `conpty_soak_native_host_not_isolated`; o controle imediato passou 3 rodadas/12 sessões, 12 root/descendant/native-host PIDs distintos, 50 eventos, `noOrphans` e fixture removida, e a repetição integral passou `193/193`. `D-013` preserva todas as ocorrências; nenhum código P3 foi alterado nesta PR.

## Limites

- o writer é a fronteira contínua do stream e precisa sobreviver a todos os chunks do record limitado; exceder o teto falha sem persistir o pendente;
- autenticação da identidade que fornece `writerId`/`readerId` pertence ao boundary confiável que integrará o store em P4-PR04; esta unidade aplica a autorização recebida e não cria transporte;
- replay/cursores/reidratação de cliente: P4-PR03;
- sanitização da conversão de resultados hostis do relógio: `D-014`, destinada à P4-PR03;
- leitura do snapshot pela mesma identidade validada, eliminando TOCTOU `lstat`/`readFile`: `D-015`, destinada à P4-PR03;
- coerência de `redactionCount` reidratado com conteúdo/stream: `D-016`, destinada à P4-PR03;
- correlação da lease com identidade recuperável da instância do processo, não apenas PID: `D-017`, destinada à P4-PR03;
- produtores/API de sessões reais: P4-PR04;
- dashboard/terminal renderer: P5.

## Parecer local

`P4-PR02: GREEN_CANDIDATE` na PR #18, base `3657a070e5dc6b1e7b78fa1804761440c55efffc`. A revisão independente bloqueou `1d93161` por quote PowerShell escapada, e os P1 do mesmo head para terminal/prototype/cursor/YAML foram reproduzidos. `79382d4` corrige as categorias estruturalmente com chunks, retorno live, `inspect()`, disco e negativos/dedent. Focused 21/21 e suíte repetida 193/193 estão verdes. Submeter o novo head exato a outra sessão independente; não fazer merge antes desse gate.
